# VITE_BACKEND_URL → Vite proxy target only (server-side, no CORS) for the development 
# VITE_API_URL     → direct browser fetch (leave unset to use proxy) for the deploying

#VITE_API_URL=https://cuesync-b.musicrightsmanagement.in
VITE_BACKEND_URL=http://localhost:8000
VITE_APP_NAME=CueSync
